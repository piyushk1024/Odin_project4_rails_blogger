<h1> All Tags </h1>

<ul id = "Tags">
  <% @tags.each do |tags| %>
  <li>
    <%= link_to tags.name, tag_path(tags), class:'tag_title'%>
  </li>
  <%end%>
</ul>

<%= link_to "<<Back to Articles List", articles_path %>
